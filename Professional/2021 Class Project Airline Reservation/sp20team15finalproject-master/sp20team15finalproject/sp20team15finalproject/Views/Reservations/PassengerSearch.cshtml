@model sp20team15finalproject.Models.AppUser

@{
    ViewData["Title"] = "View";
}

<h1>Users</h1>

@* <table class="table">
    <thead>
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.AdvantageNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.AdvantageNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Flight.LastName)
                </td>
                <td>
                    <a asp-action="SelectUser" asp-route-id="@item.Email">Select User</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Flight.FlightTime)
                </td>
                <td>
            </tr>
        }
    </tbody>
</table>*@